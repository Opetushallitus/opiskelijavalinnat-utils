<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0
                      http://maven.apache.org/xsd/settings-1.0.0.xsd">

    <!-- artifactory.opintopolku.fi on poistettu käytöstä. viestinvalityspalvelun pom ja
         fi.vm.sade.java-utils -riippuvuuksien pom-tiedostot tuovat sen repot mukanaan, joten
         ohjataan ne GitHub Packagesiin. Sama id kuin serverillä, jotta autentikointi periytyy
         peilatulle liikenteelle. -->
    <mirrors>
        <mirror>
            <id>github</id>
            <name>Artifactory-repot ohjataan GitHub Packagesiin</name>
            <url>https://maven.pkg.github.com/opetushallitus/*</url>
            <mirrorOf>oph-sade-artifactory,oph-sade-artifactory-snapshots,oph-sade-artifactory-releases</mirrorOf>
        </mirror>
    </mirrors>

    <activeProfiles>
        <activeProfile>github</activeProfile>
    </activeProfiles>

    <profiles>
        <profile>
            <id>github</id>
            <repositories>
                <repository>
                    <id>github</id>
                    <url>https://maven.pkg.github.com/opetushallitus/*</url>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <servers>
        <server>
            <id>github</id>
            <username>private-token</username>
            <password>${env.GITHUB_TOKEN}</password>
        </server>
    </servers>
</settings>